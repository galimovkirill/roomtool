# TODO — отложенные решения

Этот файл содержит вопросы и функциональность, которые намеренно отложены и не входят в текущий MVP.
Нужно вернуться к ним позже.

---

## Горячие клавиши

Определить и задокументировать полный перечень горячих клавиш для управления приложением.

Вопросы на проработку:
- какие клавиши управляют камерой (повороты, зум) — сейчас реализовано на усмотрение разработчика
- Delete / Backspace для удаления выбранного элемента
- ESC как дополнительный способ снять выделение
- Tab для переключения между элементами на сцене
- прочие шорткаты, типичные для дизайн-инструментов (пространство + drag для pan, и т.д.)

После проработки — перенести в `mvp.md` (must have или nice to have) и удалить отсюда.

---

## TASK-009 — Хоткеи Ctrl+Z / Ctrl+Y и управление камерой

Задача отложена для доработки. Оригинальный промпт из `BACKLOG.md`:

Создать хук `useKeyboard.ts` для подписки на клавиши и подключить Undo/Redo.

Вопросы на проработку:
- Хук принимает handlers: `Record<string, (e: KeyboardEvent) => void>`. Ключи — строки формата `'key'`, `'ctrl+key'`, `'ctrl+shift+key'`.
- Как обрабатывать `e.metaKey` (Mac Cmd) наравне с `e.ctrlKey`? На macOS стандартные хоткеи используют Cmd вместо Ctrl (Cmd+Z, Cmd+Y, Cmd+C и т.д.) — хук должен корректно работать на обеих платформах без дублирования handlers.
- Компонент `KeyboardShortcuts` в `main.tsx` — нужен ли отдельный компонент или достаточно хука в корневом компоненте?
- Полный список хоткеев (не только Ctrl+Z/Y): Delete, ESC, Tab, стрелки для поворота и т.д. — cм. раздел «Горячие клавиши» выше.
- Тесты на `useKeyboard`: как мокать `KeyboardEvent` в jsdom корректно.

После проработки — вернуть задачу в `BACKLOG.md` с обновлённым промптом.
