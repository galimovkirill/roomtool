Выполни задачу из BACKLOG.md.

## Аргумент

Если передан аргумент (например `TASK-002`) — выполни именно эту задачу.

Если аргумент не передан — определи следующую задачу автоматически:
```bash
grep -m1 "⬜" BACKLOG.md
```
Из вывода извлеки ID задачи (например `TASK-013`).

## Инструкция по выполнению задачи

1. Найди строку начала блока задачи:
   ```bash
   grep -n "### TASK-XXX" BACKLOG.md
   ```
   Используй номер строки как `offset` в инструменте Read, `limit: 80`.

2. Прочитай промпт задачи — он содержит точные требования.
3. Сверь с CLAUDE.md соглашения проекта (единицы, алиасы, стек).
4. Выполни все шаги из промпта строго по порядку.

## Проверка качества (обязательно после каждой задачи)

После выполнения всех шагов запускай три проверки по порядку и исправляй все ошибки:

1. **Линтер:** `pnpm lint`
   - Ошибки → `pnpm lint:fix`, затем дочисти вручную
2. **Тайп-чек:** `pnpm exec tsc --noEmit`
3. **Тесты:** `pnpm test --run`

**Покрытие новой логики.** Если задача добавила или изменила код в `store/`, `utils/`, `catalog/` — напиши или обнови тесты для затронутых функций. Исключение: компоненты R3F (`scene/`) — Three.js не работает в jsdom, тестируй их только вручную через Playwright. После написания тестов прогони `pnpm test:coverage` и убедись, что пороги не упали.

Только после прохождения всех трёх переходи к браузерной проверке.

## Браузерная проверка (для UI/visual задач)

**Пропусти** для задач с чисто логическими изменениями (store, utils, hooks, тесты, конфиги).

**Выполни** если задача создаёт/меняет компоненты в `scene/`, `panels/`, `ui/`, или затрагивает Canvas/Three.js/CSS-layout.

Запусти через **Agent** (subagent_type: general-purpose) — это изолирует Playwright-вызовы от основного контекста. Передай агенту:

```
Проверь работу приложения RoomTool (рабочая директория: /Users/kirillgalimov/projects/roomtool).

1. Запусти `pnpm dev` в фоне, подожди 3 сек, определи порт из лога.
2. Открой приложение через mcp__playwright__browser_navigate.
3. Проверь консоль на ошибки (не считай предупреждения THREE.Clock ошибкой).
4. [Опиши конкретное действие для проверки задачи — что добавить, на что кликнуть]
5. Сделай скриншот в `/tmp/` (НЕ в корень проекта, не в `src/`).
6. Убей dev-сервер: kill $(lsof -ti:PORT).
7. Верни: есть ли JS-ошибки, виден ли ключевой результат (опиши что видно на скриншоте).
```

## Ревью (обязательно после проверки качества)

После прохождения lint/tsc/тестов — запусти `/review` через Skill tool (`skill: "review"`).

**Исправляй все замечания с оценкой ⚠️ и ❌.** После каждого исправления повторно запускай `pnpm lint && pnpm exec tsc --noEmit && pnpm test --run`.

Только косметические замечания, явно помеченные агентом как необязательные, можно пропустить.

## Синхронизация документации (Definition of Done)

Перед тем как сообщить о завершении — проверь, затронула ли задача что-либо из:

- **Типы** (`types/index.ts`) — новые/изменённые интерфейсы
- **Конфиги и константы** (`config/`, `catalog/`) — новые поля, категории, материалы
- **Store или utils** — новые мутации, алгоритмы, паттерны
- **Структура компонентов** — новые файлы в `components/`, переименования
- **Конвенции** — новые договорённости, исключения, известные ограничения
- **Тесты** — новые паттерны или правила покрытия

Если хотя бы один пункт задет — открой `CLAUDE.md` и обнови соответствующие разделы.
**Обновляй только «почему» и договорённости, не пересказывай код.**
Дерево файлов обновляй только при добавлении/удалении файлов, а не при переименовании функций.

## После выполнения

1. В BACKLOG.md в "Сводной таблице задач" замени ⬜ на ✅
2. Сообщи пользователю: какая задача выполнена, что сделано, что нашёл ревью и что исправлено, какая следующая
3. **Не делай коммит** — пользователь сделает через `/commit`
4. **Не переходи к следующей задаче** — жди команды

## Текущий аргумент

$ARGUMENTS
